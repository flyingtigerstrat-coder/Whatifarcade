# Generated by the IRONLINE tools — regenerable, never committed (see IRONLINE_TOOLS.md §3).
# exporter (ironline-export.js) render outputs:
ironline-*.png
# importer (ironline-import.py) outputs (the embed snippet is pasted into the game, not committed):
sprite-*.png
sprite-*.js
