# services/saves — THE WIRE · Cloudflare Worker config
# Deploy path A (recommended, no secrets): Cloudflare dashboard -> Workers & Pages -> Create ->
#   import the GitHub repo -> set root directory to services/saves -> deploy. See README.md.
# The KV namespace id below is a PLACEHOLDER: create the namespace first (dashboard ->
#   Storage & Databases -> KV -> Create namespace "wia-saves") and paste its id here.

name = "wia-saves"
main = "worker.js"
compatibility_date = "2026-06-01"

[[kv_namespaces]]
binding = "SAVES"
id = "f50a5ffb9c1442c4ac739ec586ff9624"
